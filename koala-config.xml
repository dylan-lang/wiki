<?xml version="1.0"?>
<koala>
  <listener address="0.0.0.0" port="8000"/>
  <debug-server value="on" />

  <!-- dev mode makes DSP check for modified templates and reparse them. -->
  <development-mode enabled="on" />

  <server-root location="c:/cgay/dylan/trunk/libraries/network/wiki"/>

  <!-- For now we set the document root and dsp root explicitly.
       They are not relative to server root due to the way the wiki is
       started up. -->
  <document-root location="c:/cgay/dylan/trunk/libraries/network/wiki/www"/>
  <dsp-root location="c:/cgay/dylan/trunk/libraries/network/wiki/www/dsp"/>

  <wiki content-directory = "c:/cgay/dylan/trunk/libraries/network/wiki/storage" />
</koala>
